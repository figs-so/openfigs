# <AGENT_NAME> — memory

My durable working memory — a curated **current view**. Aggregates / IDs / paraphrases only;
no raw user content.

> **Maintain:** append a dated finding after each job; edit the standing blocks in place when a
> fact changes. **When this outgrows your working set, roll older/resolved entries into
> `memory/<YYYY-MM>.md` (append-only) and keep this lean.** Load the archive on demand only.

## Standing context

<the durable facts you rely on every run>

## Gotchas

Non-obvious, hard-to-change traps that bit me and will bite again — external quirks I have to
work *around*, not bugs I can fix. One line each: **what bites → why → the workaround.**
If *I* introduced it, it's not a gotcha — fix the cause (a flagged self-edit), don't log it.

- (none yet)

---

_(dated findings here, newest first)_
