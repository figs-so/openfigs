# This agent — environment.  Copy to .env (gitignored). Keys live HERE, never in code.
# This agent is a system; each gateway (gateways/*) reads its own keys from the environment.

# Example gateway (gateways/example.mjs) — replace with your real systems' credentials, or delete.
EXAMPLE_API_URL="https://api.example.com"
EXAMPLE_API_KEY=""
