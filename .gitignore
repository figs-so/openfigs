# Secrets — never commit credentials or env files
.env
.env.local
**/.figs/credentials.json

# Figs journal — machine-local; pushed to Figs, aggregated remotely, not source history.
# (config.json — your committed identity — and agent.json / CONTRACT.md are NOT ignored.)
**/.figs/runs.jsonl
**/.figs/asks.jsonl
**/.figs/messages.jsonl
**/.figs/artifacts/

# Per-user local runtime settings (not shared)
**/.claude/settings.local.json

# Internal strategy/positioning notes — never publish (this repo is MIT/public-bound)
POSITIONING.md

# Node / OS / scratch
node_modules/
.DS_Store
*.out
scratch/
