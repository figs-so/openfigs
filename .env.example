# OpenFigs — gateway credentials. Copy to .env (gitignored).
# Your gateways (gateways/*) read their keys from the environment — never embed them.

EXAMPLE_API_URL="https://api.example.com"
EXAMPLE_API_KEY=""
