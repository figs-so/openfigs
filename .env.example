# OpenFigs — environment.
# Copy to .env (gitignored). Keys live HERE, never embedded in agent code.
# Each capability door (lib/doors/*) reads its own keys from the environment.

# Example door (lib/doors/example.mjs) — replace with your real systems' credentials.
EXAMPLE_API_URL="https://api.example.com"
EXAMPLE_API_KEY=""
