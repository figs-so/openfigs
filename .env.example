# OpenFigs — shared-service credentials (root). Copy to .env (gitignored).
# Shared services in services/* read their keys from the environment. An agent's OWN
# gateway keys live in that agent's .env instead.

EXAMPLE_API_URL="https://api.example.com"
EXAMPLE_API_KEY=""
